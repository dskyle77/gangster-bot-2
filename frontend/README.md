# GANGSTER BOT — Dashboard

Simple Next.js dashboard. Reads from the Express API on the backend.

## Setup
```bash
cd frontend
npm install
# optional: NEXT_PUBLIC_API_URL=http://localhost:3001
npm run dev
```

Open http://localhost:3000

Backend must be running on port 3001.
